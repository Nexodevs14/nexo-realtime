/**
 * Event types for Conditional realtime events.
 */
export enum ConditionalEventType {
  CREATED = 'conditional.created',
  UPDATED = 'conditional.updated',
  DELETED = 'conditional.deleted',
}

/**
 * Event types for Conditional Requirement realtime events.
 */
export enum ConditionalRequirementEventType {
  CREATED = 'conditional_requirement.created',
  UPDATED = 'conditional_requirement.updated',
  DELETED = 'conditional_requirement.deleted',
}
