/**
 * Event type enum for Audit Execution realtime.
 */
export enum AuditExecutionEventType {
  CREATED = 'created',
  UPDATED = 'updated',
  STATUS_CHANGED = 'status_changed',
  DELETED = 'deleted',
}
